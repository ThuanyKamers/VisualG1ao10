Algoritmo "exercicio7"

Var

dia, mes, total_dias : inteiro

Inicio

escreva("Digite o dia: ")
leia(dia)
escreva("Digite o mês: ")
leia(mes)

total_dias <- (mes - 1) * 30 + dia

escreval("Dias que se passaram desde o início do ano: ", total_dias)

Fimalgoritmo